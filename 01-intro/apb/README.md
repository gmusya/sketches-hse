# Сложение чисел

Эта задача предназначена для знакомства с тестирующей системой. Вам предстоит реализовать функцию `Add`, которая складывает 2 числа, в файле `add.cpp`. Перед тем, как приступить к решению, настройте окружение согласно общей инструкции (читай папку `docs` в корне репозитория).
